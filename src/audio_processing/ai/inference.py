import tqdm
import os
import datetime


def process_audio_files(classifier, base_path, window_size, threshold, stable_version, save_embeddings, save_spectrogram, model_type):

    raise NotImplementedError("La inferencia IA todavía no está migrada.")
